# =============================================================================
# Heavy ML artifacts — this site DOCUMENTS them; it must never HOST them.
# Keep these out of git and out of the published site.
# =============================================================================
*.ckpt
*.tgz
*.tar
*.pt
*.npy
*.npz
*.sdf
design_test_runs/
generated/
xtb_*/
__pycache__/

# =============================================================================
# Jekyll build output and local Bundler install path
# =============================================================================
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/
vendor/
.bundle/

# Keep Gemfile.lock committed for reproducible local installs (do not ignore it).
